{
  "name": "@schuetzenmanager/desktop",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "prepare-sidecar": "node scripts/prepare-sidecar.mjs",
    "build": "npm run prepare-sidecar && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.1"
  }
}
